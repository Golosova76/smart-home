import { LightActiveDeviceDirective } from './light-active-device.directive';

describe('LightActiveDeviceDirective', () => {
  it('should create an instance', () => {
    const directive = new LightActiveDeviceDirective();
    expect(directive).toBeTruthy();
  });
});
